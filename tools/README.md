The script that generated the four pages from versions/mockup-v3-single-file.html.
Re-running it overwrites the page files, so only use it to regenerate from the original mockup.

    python3 tools/build-from-mockup.py
